{
  "name": "eventflow",
  "private": true,
  "version": "0.0.0",
  "description": "Root dev script to run client and server together. Not a workspace root — install client/ and server/ deps separately.",
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "install:all": "npm install --prefix client && npm install --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
